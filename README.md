# distributed-event-processor

---
# 🚀 Event Processing System 

Это распределенная система обработки событий, построенная на микросервисной архитектуре. Проект демонстрирует интеграцию Java-сервисов с брокером сообщений RabbitMQ, реляционной БД (PostgreSQL) и NoSQL БД (MongoDB).

---

## 🏗 Архитектура системы

Система состоит из трех независимых микросервисов:

1.  **Collector Service**: Раз в минуту опрашивает внешний API и отправляет полученные события в RabbitMQ (очередь `raw_events`).
2.  **SQL Service**: Слушает `raw_events`, выполняет дедупликацию и сохраняет уникальные события в PostgreSQL. После сохранения отправляет сигнал в очередь `processed_events`.
3.  **Mongo Service**: Слушает `processed_events` и сохраняет финальные данные в MongoDB для аналитики и долгосрочного хранения.



---

## 🛠 Технологический стек

* **Backend**: Java 21 (Amazon Corretto), Spring Boot 3.x, Spring Data JDBC/MongoDB.
* **Messaging**: RabbitMQ (AMQP).
* **Databases**: PostgreSQL (SQL), MongoDB (NoSQL).
* **DevOps**: Docker, Docker Compose, Bash-скрипты.
* **Testing**: JUnit 5, Mockito, AssertJ.

---

## 🚀 Быстрый старт

### Требования
* Docker & Docker Compose
* JDK 21

### Локальный запуск
1. Клонируйте проект и перейдите в нужную ветку:
   ```bash
   git clone [https://github.com/eeffeecct/distributed-event-processor.git](https://github.com/eeffeecct/distributed-event-processor.git)
   cd distributed-event-processor

```

2. Соберите JAR-файлы:
```bash
./gradlew clean build (-x test)

```


3. Запустите все сервисы:
```bash
docker compose up -d

```



---

## 🌐 Деплой на сервер (CI/CD Bash)

Для автоматизации развертывания на удаленном сервере (Ubuntu) используется скрипт `deploy.sh`.

**Скрипт выполняет:**

* Авторизацию в GitHub по токену.
* Сборку проекта на сервере с использованием Amazon Corretto 21.
* Запуск в режиме **Production** через `docker-compose.prod.yml`.
* Обеспечение порядка запуска через **Healthchecks** (сервисы ждут готовности RabbitMQ и БД).

**Команда для запуска:**

```bash
chmod +x deploy.sh
./deploy.sh

```

---

## 🧪 Тестирование

В проекте реализовано модульное тестирование с использованием Mockito для проверки бизнес-логики в изоляции:

* **Unit-тесты**: Проверка маппинга DTO в Entity, логика дедупликации, обработка ошибок API.
* **Graceful Shutdown**: Настроено плавное завершение работы сервисов (30 сек), чтобы избежать потери сообщений в RabbitMQ.

Запуск тестов:

```bash
./gradlew test

```

---

## 📈 Мониторинг логов

Для отслеживания пути сообщения через все сервисы:

```bash
sudo docker compose -f docker-compose.prod.yml logs -f

```

**Автор:** [eeffeecct](https://github.com/isicju)

**Project:** Java Course 2025
